CREATE TABLE [dbo].[SecurityQuestion] (
    [SecurityQuestionId]     INT          IDENTITY (1, 1) NOT NULL,
    [SecurityQuestionName] VARCHAR (50) NULL,
    PRIMARY KEY CLUSTERED ([SecurityQuestionId] ASC)
);

